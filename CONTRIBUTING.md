# Contributing

Keep routing changes small and cover both the matching result and the
no-match path. Preserve the package's existing runtime and type declarations;
update examples when a public API changes.

Run the package's formatter, type check, and tests before submitting a pull
request.
